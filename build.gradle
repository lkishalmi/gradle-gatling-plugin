buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
    dependencies {
        classpath "com.gradle.publish:plugin-publish-plugin:0.9.2"
    }
}
apply plugin: 'java-gradle-plugin'
apply plugin: 'groovy'
apply plugin: "com.gradle.plugin-publish"

group = 'com.github.lkishalmi.gradle'
version = '0.1'

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

dependencies {
    testCompile gradleTestKit()
}

test {
    systemProperty 'pluginClasspath', "${sourceSets.main.output.classesDir}:${sourceSets.main.output.resourcesDir}"
    systemProperty 'testResources', "${sourceSets.test.output.resourcesDir}"
}

pluginBundle {
    website = 'https://github.com/lkishalmi/gradle-gatling-plugin'
    vcsUrl = 'https://github.com/lkishalmi/gradle-gatling-plugin'
    description = 'Gatling Simulation Execution'
    tags = ['gradle-plugin', 'gatling', 'load test', 'stress test', 'preformance test', 'scala']

    plugins {
        gatlingPlugin {
            id = 'com.github.lkishalmi.gatling'
            displayName = 'Gatling Simulation Execution'
        }
    }
}